#!/usr/bin/env bash
#MISE description="Run all tests"
#USAGE flag --coverage default="false"

set -euo pipefail

# Run tests without race detection on all packages due to DuckDB compatibility issues
# Race detection can cause false positives with DuckDB's C++ bindings
go test -v ./...
